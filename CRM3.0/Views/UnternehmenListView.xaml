<UserControl x:Class="CRM.Views.UnternehmenListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Überschrift und Button-Bereich -->
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Unternehmen" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Margin="0,0,0,10"/>
            <Button Content="+ Neues Unternehmen" 
                    Width="150" 
                    HorizontalAlignment="Left"/>
        </StackPanel>

        <!-- Liste der Unternehmen -->
        <DataGrid Grid.Row="1" 
                  x:Name="UnternehmenDataGrid"
                  Margin="10"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Firmenname" Binding="{Binding Firmenname}" Width="*"/>
                <DataGridTextColumn Header="Rechtsform" Binding="{Binding Rechtsform}" Width="100"/>
                <DataGridTextColumn Header="Ort" Binding="{Binding Ort}" Width="150"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Telefon}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>