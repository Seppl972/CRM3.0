<UserControl x:Class="CRM.Views.VersicherterFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Überschrift -->
        <TextBlock Grid.Row="0" 
                   Text="Neuer Versicherter" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Formular -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Vorname *" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="VornameTextBox" Height="30"/>

                <TextBlock Text="Nachname *" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="NachnameTextBox" Height="30"/>

                <TextBlock Text="Firma" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="FirmaTextBox" Height="30"/>

                <TextBlock Text="Email" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="EmailTextBox" Height="30"/>

                <TextBlock Text="Telefon" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="TelefonTextBox" Height="30"/>

                <TextBlock Text="Mobil" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="MobilTextBox" Height="30"/>

                <TextBlock Text="Straße" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="StraßeTextBox" Height="30"/>

                <TextBlock Text="PLZ" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="PLZTextBox" Height="30"/>

                <TextBlock Text="Ort" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="OrtTextBox" Height="30"/>

                <TextBlock Text="Notizen" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox x:Name="NotizenTextBox" 
                         Height="80" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="Abbrechen" 
                    Width="100" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Click="AbbrechenButton_Click"/>
            <Button Content="Speichern" 
                    Width="100" 
                    Height="35"
                    Click="SpeichernButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>